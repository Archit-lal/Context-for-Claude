# Periphery

Static site for Periphery. Plain HTML, no build step.

- `index.html` — the whole site
- `favicon.svg` — the mark
- `.github/workflows/deploy.yml` — deploys to GitHub Pages on every push to `main`

Preview locally:

```sh
python3 -m http.server 8000
```
